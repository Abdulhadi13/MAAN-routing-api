# Host port used to expose the FastAPI service.
API_PORT=8000

# PostGIS credentials shared by the database and API containers.
POSTGRES_DB=maan_routing
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
