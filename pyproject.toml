[project]
name = "maan-routing-api"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.135.1",
    "httpx>=0.27.0",
    "pytest>=9.0.2",
]
